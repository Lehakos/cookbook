import React from 'react';

import * as s from './{{ kebabCase name }}.styles';

interface Props {}

export const {{ pascalCase name }}: React.FunctionComponent<Props> = function() {
  return <s.Wrapper>{{pascalCase name}}</s.Wrapper>;
};
